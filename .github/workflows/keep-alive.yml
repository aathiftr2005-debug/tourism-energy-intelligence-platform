name: Keep Alive

on:
  schedule:
    - cron: '*/14 * * * *'

jobs:
  ping:
    name: Ping Backend
    runs-on: ubuntu-latest
    steps:
      - name: Health check
        run: curl -sSf ${{ secrets.PING_URL || 'http://localhost:8000/health' }} || true
